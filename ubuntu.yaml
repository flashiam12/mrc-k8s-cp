apiVersion: v1
kind: Pod
metadata:
  labels:
    app: ubuntu-kafka
  name: ubuntu-kafka
  namespace: default
spec:
  containers:
  - command:
    - sleep
    - "604800"
    image: python:3.11.4-bullseye
    imagePullPolicy: IfNotPresent
    name: ubuntu
